# iOS app

The iOS app lives in its **own repository** (already exists and runs). This folder is a placeholder for cross-repo notes and shared context.

## To fill in

- **Repo URL**: _TBD_
- **Stack**: _TBD (Swift/SwiftUI, React Native, Flutter, ...)_
- **How brand assets flow in**: when `brand/` is updated, export the relevant files (app icon, in-app logos, color tokens) into the iOS repo. Document the export path here once the stack is confirmed.
- **Release process**: _TBD_

Update this README the next time you sit down with the iOS app.
