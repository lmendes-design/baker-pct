# Ideas — Baker Percentage

A running log of things worth remembering. Newest entries at the top.

Add an entry from Claude Code: `/add-idea <your idea>` (or just `/add-idea` and Claude will ask). The command timestamps the entry and inserts it right below this header.

---
